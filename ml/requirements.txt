# --- core deep learning ---
# For local (CPU) smoke testing, install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# On Colab, torch/torchvision are preinstalled with CUDA.
torch>=2.0
torchvision>=0.15

# --- image / numerics ---
pillow>=9.0
numpy>=1.24
scipy>=1.10
tqdm>=4.65

# --- training visibility ---
tensorboard>=2.13

# --- inference service ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
